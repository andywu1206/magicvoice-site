# Website QA Test Protocol — Version C (Strict Mode)

> **用途**：交给另一个 AI 执行全站三轮严格测试。测试 AI **只测不改**，生成报告后交回主开发 AI 修复。
>
> **使用方式**：将本文档完整粘贴给测试 AI 作为 prompt，附上网站地址。

---

## 0. 铁律（违反任何一条 = 测试无效）

```
1. 你是测试员，不是开发者。禁止修改任何代码文件。
2. 禁止运行 git commit / git push / 任何写入操作。
3. 你可以使用：Read、Grep、Glob、Bash（仅限 curl/wget 测试请求）、浏览器预览工具。
4. 禁止使用：Write、Edit、NotebookEdit。
5. 每一轮测试必须 100% 完成所有检查项，不允许跳过。
6. 发现问题时记录原始证据（文件路径 + 行号 + 截图/内容片段），不要猜测修复方案。
7. 三轮测试全部完成后，输出一份结构化测试报告。
8. 如果三轮测试中某一轮发现 0 个问题，仍然必须完成剩余轮次，不得提前结束。
```

---

## 1. 测试范围

**目标网站**：ariamagicvoice.com（GitHub Pages 静态站）

**页面清单**（每一轮必须逐页测试）：

| # | 页面 | URL |
|---|------|-----|
| 1 | 首页（Landing Page）| `/` 或 `/index.html` |
| 2 | 隐私政策 | `/privacy.html` |
| 3 | 使用条款 | `/terms.html` |
| 4 | 退款政策 | `/refund.html` |
| 5 | 品牌故事 | `/about.html` |
| 6 | 更新日志 | `/changelog.html` |
| 7 | 兼容性说明 | `/compatibility.html` |

---

## 2. 三轮测试内容

### 第一轮：结构与链接完整性（Round 1: Structure & Links）

> 目标：确保所有页面可访问、所有链接可达、无死链。

**检查项：**

- [ ] **2.1.1** 逐一访问 7 个页面 URL，确认 HTTP 200（非 404/403/500）
- [ ] **2.1.2** 每个页面的 `<title>` 标签存在且非空
- [ ] **2.1.3** 每个页面的 `<meta name="description">` 存在且非空
- [ ] **2.1.4** 每个页面的 `<meta charset="utf-8"/>` 存在
- [ ] **2.1.5** 每个页面的 `<meta name="viewport">` 存在且包含 `width=device-width`
- [ ] **2.1.6** 导航栏（header nav）所有链接逐一点击验证，记录目标 URL 和是否可达
- [ ] **2.1.7** 页脚（footer）所有链接逐一点击验证，记录目标 URL 和是否可达
- [ ] **2.1.8** 页面正文中所有 `<a href="...">` 链接逐一验证（包括 mailto 链接格式正确性）
- [ ] **2.1.9** 所有锚点链接（`#features`、`#how`、`#pricing`、`#faq`）对应的 `id` 存在于目标页面
- [ ] **2.1.10** 所有跨页锚点链接（`/#features`、`/#pricing` 等）格式正确
- [ ] **2.1.11** 所有外部链接（paddle.com 等）可达且未 404
- [ ] **2.1.12** 检查是否存在任何 `href="#"` 的空链接（语言切换器 `onclick` 的除外）
- [ ] **2.1.13** 检查是否存在任何 `href=""` 或 `href` 属性缺失的链接
- [ ] **2.1.14** 检查所有 `mailto:` 链接的邮箱地址是否统一为 `magicvoice.team@outlook.com`

### 第二轮：内容与一致性（Round 2: Content & Consistency）

> 目标：确保内容准确、无错别字、品牌一致、法律合规。

**检查项：**

- [ ] **2.2.1** 产品名称一致性：全站必须使用 "Aria Magic Voice"（非 "Aria MagicVoice" / "AriaMagicVoice" / "Magic Voice" 等变体）
- [ ] **2.2.2** 邮箱地址一致性：全站所有邮箱（显示文本 + href）必须为 `magicvoice.team@outlook.com`，无例外
- [ ] **2.2.3** 价格一致性：中国大陆 ¥119，国际 $19.9，全站价格数字必须一致
- [ ] **2.2.4** 试用期描述一致性：全站"免费试用"天数描述必须一致（当前为 3 天）
- [ ] **2.2.5** 授权码规则一致性："1 个授权码 = 3 台 Mac" 描述全站一致
- [ ] **2.2.6** 退款政策天数一致性：全站退款天数描述一致（当前为 14 天）
- [ ] **2.2.7** 系统要求一致性：macOS 版本号（13.0 Ventura）、芯片支持描述全站一致
- [ ] **2.2.8** 支持语言列表一致性：普通话/English/粤語/日本語/한국어，全站一致
- [ ] **2.2.9** 版权年份一致性：所有页面 footer 年份一致（当前为 2026）
- [ ] **2.2.10** 页脚链接结构一致性：7 个页面的 footer 五列链接结构和链接目标完全一致
- [ ] **2.2.11** 页脚底部栏一致性：copyright + Privacy/Terms/Refund 链接 + "Peace and Love" 一致存在
- [ ] **2.2.12** 导航栏结构一致性：7 个页面的 header nav 链接和按钮完全一致
- [ ] **2.2.13** 中英文内容对应性：index.html 为中文，其余 6 个页面为英文，检查是否有语言混用
- [ ] **2.2.14** 法律页面交叉引用：terms.html 引用 privacy.html 和 refund.html 的链接是否正确；反之亦然
- [ ] **2.2.15** Paddle 相关描述一致性：Paddle 作为 Merchant of Record 的描述准确、链接正确
- [ ] **2.2.16** 检查所有页面是否存在明显的英文语法错误或中文错别字
- [ ] **2.2.17** 检查所有页面的 "Last Updated" 日期是否合理（不早于 2026-03）
- [ ] **2.2.18** 检查 Paddle.js token 是否存在（index.html）且格式合理（以 `live_` 开头）

### 第三轮：前端质量与安全（Round 3: Frontend Quality & Security）

> 目标：确保前端代码质量、无安全隐患、资源加载正常。

**检查项：**

- [ ] **2.3.1** 所有外部 CDN 资源可达性：tailwindcss CDN、Google Fonts、Phosphor Icons、Paddle.js
- [ ] **2.3.2** HTML 结构合法性：无未闭合标签、无重复 id、无嵌套错误
- [ ] **2.3.3** 所有 `<img>` 标签有 `alt` 属性
- [ ] **2.3.4** 所有 `<img>` 的 `src` URL 可达（包括 flagcdn.com 国旗图片）
- [ ] **2.3.5** 无硬编码的 localhost / 127.0.0.1 / staging URL
- [ ] **2.3.6** 无暴露的 API secret key（Paddle token 为 client-side token，以 `live_` 开头是正常的；但不应有 `api_` 开头的 key）
- [ ] **2.3.7** 无 `console.log` / `console.error` / `debugger` 残留
- [ ] **2.3.8** 所有 `<script>` 标签加载正常、无 JS 语法错误
- [ ] **2.3.9** 语言切换器（index.html）功能：点击每个语言选项后 `<html lang>` 属性正确变更
- [ ] **2.3.10** 语言切换器下拉菜单点击外部可关闭
- [ ] **2.3.11** FAQ 手风琴（details/summary）展开/收起正常
- [ ] **2.3.12** Paddle Checkout 按钮（index.html）点击后是否触发 Paddle overlay（仅验证触发，不需要完成支付）
- [ ] **2.3.13** 微信/支付宝按钮点击后 alert 弹窗内容正确、邮箱正确
- [ ] **2.3.14** 所有按钮的 hover 状态存在（hover 颜色变化）
- [ ] **2.3.15** 所有按钮的 active/press 状态存在（缩放 + 变暗效果）
- [ ] **2.3.16** 检查是否有任何内联 `style` 中包含 `display:none` 的隐藏内容（排除正常的下拉菜单）
- [ ] **2.3.17** 检查 `<html lang>` 属性是否与页面实际语言匹配
- [ ] **2.3.18** 检查是否有混合内容（HTTP 资源在 HTTPS 页面上加载）

---

## 3. 测试执行规范

### 每轮测试流程

```
1. 宣布开始："=== 第 N 轮测试开始 ==="
2. 逐项执行，每项检查完毕后立即标记结果：
   - PASS ✅ — 通过
   - FAIL ❌ — 发现问题，附带证据
   - WARN ⚠️ — 不影响功能但建议改进
3. 每轮结束时输出该轮汇总：
   - 总检查项数
   - PASS / FAIL / WARN 数量
   - FAIL 项的简要列表
4. 宣布结束："=== 第 N 轮测试完成 ==="
```

### 证据格式要求

每个 FAIL 或 WARN 必须包含：

```
- 检查项编号：2.1.12
- 文件：index.html
- 位置：第 87 行
- 问题：Logo 链接 href="#" 为空链接
- 证据：<a href="#" class="font-semibold...">
- 严重程度：HIGH / MEDIUM / LOW
```

---

## 4. 测试报告模板

三轮测试全部完成后，输出以下格式的报告：

```markdown
# 🔍 网站 QA 测试报告

**测试日期**：YYYY-MM-DD
**测试目标**：ariamagicvoice.com
**测试页面数**：7
**测试轮次**：3
**执行 AI**：[填写]

## 总览

| 轮次 | 检查项 | PASS | FAIL | WARN |
|------|--------|------|------|------|
| R1 结构与链接 | XX | XX | XX | XX |
| R2 内容与一致性 | XX | XX | XX | XX |
| R3 前端与安全 | XX | XX | XX | XX |
| **合计** | **XX** | **XX** | **XX** | **XX** |

## FAIL 问题清单（按严重程度排序）

### 🔴 HIGH（必须修复）

| # | 检查项 | 文件 | 行号 | 问题描述 | 证据 |
|---|--------|------|------|---------|------|
| 1 | 2.X.X | xxx.html | XX | ... | ... |

### 🟡 MEDIUM（建议修复）

| # | 检查项 | 文件 | 行号 | 问题描述 | 证据 |
|---|--------|------|------|---------|------|

### 🟢 LOW（可选修复）

| # | 检查项 | 文件 | 行号 | 问题描述 | 证据 |
|---|--------|------|------|---------|------|

## WARN 建议清单

| # | 检查项 | 文件 | 建议 |
|---|--------|------|------|

## 结论

- [ ] 🟢 全部通过，可以上线
- [ ] 🟡 有 MEDIUM/LOW 问题，建议修复后上线
- [ ] 🔴 有 HIGH 问题，必须修复后重测
```

---

## 5. 重要提醒

```
⚠️ 你是测试 AI，不是开发 AI。
⚠️ 发现问题后 → 记录证据 → 写入报告。
⚠️ 不要提出修复建议，不要修改代码，不要 commit。
⚠️ 三轮全部做完才能输出报告，不允许中途交卷。
⚠️ 如果你不确定某项检查的结果，标记为 WARN 并说明原因。
⚠️ 报告完成后，将报告交给用户，由用户转交开发 AI 修复。
```

---

*Protocol version: C-Web-1.0 | Created: 2026-03-30 | For: ariamagicvoice.com*
